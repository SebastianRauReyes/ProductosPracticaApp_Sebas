<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rau.sebastian.productospracticaapp_sebas">

    <application
        android:name="com.orm.SugarApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="DATABASE"
            android:value="sugarormapp.db" />
        <meta-data
            android:name="VERSION"
            android:value="1" />
        <meta-data
            android:name="QUERY_LOG"
            android:value="true" />
        <meta-data
            android:name="DOMAIN_PACKAGE_NAME"
            android:value="com.rau.sebastian.productospracticaapp_sebas.models" />

        <activity
            android:name=".activities.LoginActivity"
            android:theme="@style/AppTheme2">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.RegisterActivity"
            android:label="@string/app_name_user" />
        <activity android:name=".activities.DashboardActivity" />
        <activity
            android:name=".activities.RegisterProductActivity"
            android:label="@string/app_name_product" />
        <activity android:name=".fragments.FragmentFavoritesActivity" />
        <activity android:name=".fragments.FragmentArchivedActivity" />
        <activity android:name=".fragments.FragmentHomeActivity"></activity>
    </application>

</manifest>